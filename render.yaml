services:
  - type: web
    name: amoostore
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production
      # Render provides the service port at runtime via the $PORT env var; do not hard-code it here
